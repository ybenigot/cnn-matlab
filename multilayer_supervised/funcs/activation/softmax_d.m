function d=softmax_d(z)
% derivative of softmax 
% see http://math.stackexchange.com/questions/945871/derivative-of-softmax-loss-function

error('N/A : we should define the Jacobian matrix')
%	den = (1 + exp(-z));
%	d = (1 ./ den) .* (1 - 1 ./ den);

end%function